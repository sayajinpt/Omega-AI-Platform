/** User-facing brand (Greek Ω is the product symbol). Technical ids stay `omega`. */

export const BRAND_SYMBOL = 'Ω'
export const BRAND_NAME = `${BRAND_SYMBOL}mega`
export const BRAND_TAGLINE = 'Local AI operating system'
export const BRAND_FULL = `${BRAND_NAME} — ${BRAND_TAGLINE}`

/** Prefix for terminal / installer prompts */
export const BRAND_CLI_USER = `${BRAND_NAME.toLowerCase()}@installer`
