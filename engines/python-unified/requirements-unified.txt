# Omega unified Python venv — single environment (~/.omega/venvs/unified)
# Installed by C++ venv_setup (POST /v1/python/setup). Profiles add optional stacks.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
Pillow>=10.0.0
pydantic>=2.0
pydantic-settings>=2.0

numpy>=1.26.0
pyyaml>=6.0

# Stealth browser fetch (Playwright)
playwright>=1.40.0
